export const reserve = () => {
  // Trigger this when the session has been validated when the payment is successfully.

  // Frontend validates the session and obtains the reservation Ids from the metadata.

  // Session contains

  // THIS email is trying to Reserve

  // THIS reservation
  // Get the reservation in status incomplete update to complete

  // The reservation contains already the slot info.
  // get the slot. Update the attendees

  // Return the reservation and the Slot Information.

  return true;
};
